monolog:
    # One rotating file per channel, in every environment, so the back-office
    # viewer (/admin/logs) can offer each kind of log as its own section and
    # each can be downloaded on its own. Defined here rather than under
    # when@prod so the page also works while developing locally.
    #
    # These are a convenience copy for the viewer: in production the primary
    # stream is still stdout/stderr (`docker logs`), and these files live in
    # the container, so they rotate daily and are dropped after 7 days.
    handlers:
        checkout_file:
            type: rotating_file
            path: "%kernel.logs_dir%/checkout.log"
            max_files: 7
            level: info
            formatter: monolog.formatter.json
            channels: [checkout]

        payment_file:
            type: rotating_file
            path: "%kernel.logs_dir%/payment.log"
            max_files: 7
            level: info
            formatter: monolog.formatter.json
            channels: [payment]

        shipping_file:
            type: rotating_file
            path: "%kernel.logs_dir%/shipping.log"
            max_files: 7
            level: info
            formatter: monolog.formatter.json
            channels: [shipping]

        audit_file:
            type: rotating_file
            path: "%kernel.logs_dir%/audit.log"
            max_files: 7
            level: info
            formatter: monolog.formatter.json
            channels: [audit]

        mail_file:
            type: rotating_file
            path: "%kernel.logs_dir%/mail.log"
            max_files: 7
            level: info
            formatter: monolog.formatter.json
            channels: [mail]

        # Everything that is not a business channel: framework, security,
        # doctrine and application errors. Warning and above only — this is the
        # "something went wrong" file, not a transcript.
        app_file:
            type: rotating_file
            path: "%kernel.logs_dir%/app.log"
            max_files: 7
            level: warning
            formatter: monolog.formatter.json
            channels: ["!event", "!deprecation", "!checkout", "!payment", "!shipping", "!audit", "!mail"]

    channels:
        - deprecation # Deprecations are logged in the dedicated "deprecation" channel when it exists
        # Business channels. Each is autowirable as LoggerInterface $<name>Logger
        # (e.g. $paymentLogger) and is routed to its own handler in prod so a
        # successful business event survives — see the "business" handler below.
        - checkout   # cart -> address -> shipping -> payment funnel
        - payment    # Stripe and PayPal: intents, captures, verification outcomes
        - shipping   # Shippo rate lookups and label data
        - audit      # who changed what in the back-office
        - mail       # transactional email dispatch

when@dev:
    monolog:
        handlers:
            main:
                type: stream
                path: "%kernel.logs_dir%/%kernel.environment%.log"
                level: debug
                channels: ["!event"]
            console:
                type: console
                process_psr_3_messages: false
                channels: ["!event", "!doctrine", "!console"]

when@test:
    monolog:
        handlers:
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                channels: ["!event"]
            nested:
                type: stream
                path: "%kernel.logs_dir%/%kernel.environment%.log"
                level: debug

when@prod:
    parameters:
        # Verbosity is an env var so it can be raised on a running deploy without
        # rebuilding the image. The default keeps deploys whose .env predates this
        # variable booting unchanged — the server keeps its own .env, separate
        # from the one in the repo.
        app.log_level_default: 'info'
        app.log_level: '%env(default:app.log_level_default:LOG_LEVEL)%'

    monolog:
        handlers:
            # ── Business events ────────────────────────────────────────────
            # Straight to stderr at info level, deliberately NOT behind
            # fingers_crossed: that handler only flushes when something fails,
            # which means a checkout that *succeeds* would leave no trace and
            # orders could not be counted from logs. This handler is the record
            # of what the business actually did.
            business:
                type: stream
                path: php://stderr
                level: info
                formatter: monolog.formatter.json
                channels: [checkout, payment, shipping, audit, mail]

            # (The per-channel files are declared once at the top of this file
            # and apply to every environment.)

            # ── Technical errors ───────────────────────────────────────────
            # Everything else stays buffered: nothing is written until an error
            # occurs, at which point the preceding records provide the context.
            # `doctrine` is excluded because that buffer would otherwise dump
            # SQL *with bound parameters* — customer emails, addresses, names —
            # into the log store on every error.
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                channels: ["!deprecation", "!doctrine", "!checkout", "!payment", "!shipping", "!audit", "!mail"]
                buffer_size: 50 # How many messages should be saved? Prevent memory leaks
            nested:
                type: stream
                path: php://stderr
                level: '%app.log_level%'
                formatter: monolog.formatter.json

            # Doctrine errors still matter — just not the buffered SQL trail.
            doctrine_errors:
                type: stream
                path: php://stderr
                level: error
                formatter: monolog.formatter.json
                channels: [doctrine]

            console:
                type: console
                process_psr_3_messages: false
                channels: ["!event", "!doctrine"]
            deprecation:
                type: stream
                channels: [deprecation]
                path: php://stderr
                formatter: monolog.formatter.json
