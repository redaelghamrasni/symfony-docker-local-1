import type { Renderer } from '../../types';
import type { ChatLayoutOwnProps } from './types';
export declare function createChatInlineLayoutComponent({ createElement }: Renderer): (userProps: ChatLayoutOwnProps) => JSX.Element;
