import type { Connector } from '../../types';
export type FeedsRenderState = {
    feedIDs: string[];
};
export type FeedsConnectorParams = {
    /**
     * Whether feeds are isolated from the global search scope.
     * Currently only `false` is supported (future-proofing for per-feed search parameters).
     */
    isolated: false;
    /**
     * Optional: transform/reorder/filter feed IDs before rendering.
     */
    transformFeeds?: (feeds: string[]) => string[];
};
export type FeedsWidgetDescription = {
    $$type: 'ais.feeds';
    renderState: FeedsRenderState;
    indexRenderState: {
        feeds: FeedsRenderState;
    };
};
export type FeedsConnector = Connector<FeedsWidgetDescription, FeedsConnectorParams>;
declare const connectFeeds: FeedsConnector;
export default connectFeeds;
