import 'algoliasearch-helper/types/algoliasearch.js';
import { indexWidgetTypes } from './index11.js';

function isIndexWidget(widget) {
    return indexWidgetTypes.includes(widget.$$type);
}

export { isIndexWidget };
