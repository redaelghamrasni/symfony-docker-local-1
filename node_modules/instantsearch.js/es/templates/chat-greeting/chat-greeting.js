import { _ } from '@swc/helpers/esm/_object_spread.js';
import { createChatGreetingComponent } from 'instantsearch-ui-components';
import { h } from 'preact';

var ChatGreetingComponent = createChatGreetingComponent({
    createElement: h
});
function chatGreeting(options) {
    return function ChatGreetingTemplate(props) {
        return /*#__PURE__*/ h(ChatGreetingComponent, _({}, props, options));
    };
}

export { chatGreeting };
